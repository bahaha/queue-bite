package waitlist

import layout "queue-bite/internal/layouts"

type VitrinePageData struct {
	Form *JoinFormData
}

templ Vitrine(page *VitrinePageData) {
	@layout.Base() {
		@JoinForm(page.Form)
	}
}
